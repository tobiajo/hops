package old_io.hops.tensorflow;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class ApplicationMasterConf {
  
  private static final Log LOG = LogFactory.getLog(ApplicationMasterConf.class);
  
  public ApplicationMasterConf(String args[]) {
    
  }
}
